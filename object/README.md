# Block CAD

- The repo has the CAD for the block that will be used in the competition 
- `.scad` is the raw design file
- `.stl` is the rendered mesh
- The PDF has a dimensional drawing of the block face
